[tool.poetry]
name = "hunter-auto-search"
version = "0.1.0"
description = "Automated talent search for recruitment platforms (Liepin, BOSS, LinkedIn, Maimai)"
authors = ["Hermes Agent"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pydantic = "^2.0.0"
loguru = "^0.7.0"
rich = "^13.0.0"
click = "^8.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
